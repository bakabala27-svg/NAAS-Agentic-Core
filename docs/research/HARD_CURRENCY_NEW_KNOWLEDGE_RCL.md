# معرفةٌ جديدة (الدفعة 11) — RCL: تتالي السحب وسطحُ الاقتباس

## Retraction Cascade Ledger — أيُّ رقمٍ ما زال يُقتبَس بعد أن سُحِب نموذجُه؟

> **تاريخ البحث:** 2026-09-15 · **المحور:** **سلامةُ السطح القابل للاقتباس** (لا العرض
> ولا النموذج ولا المشتري ولا السكّة — بل ما تبقّى صالحاً بعد السحب) ·
> **الأداة:** [`shared/research/retraction_cascade.py`](../../shared/research/retraction_cascade.py) ·
> **الأرقام:** [`RCL_MEASUREMENTS.json`](RCL_MEASUREMENTS.json) — بصمةُ المُدخلات
> `sha256 = 6c6f89bd9746c09f68df697804744f0b8b6bedbf652bb887a0fe13bd90a7e899` ·
> **الاختبارات:** 64 (`tests/shared/test_rcl_retraction_cascade.py`)، منها براهينُ
> سلبيةٌ لـ K1–K5 · **المولِّد:** `python3 scripts/research/measure_retraction_cascade.py --check`

### ما لا يجوز الاستشهاد بهذه الوثيقة عليه

1. ⛔ **ليست رأياً قانونياً ولا ضريبياً.** كلُّ قيدٍ تنظيميٍّ هنا موسومٌ بمرجعِه
   وحالتِه، والنصُّ الحرفيُّ للجريدة الرسمية **لم يُستخرج بعد** (S_E · T77) — فكلُّ
   رقمٍ تنظيميٍّ مشروطٌ بنقلِ مصدرٍ ثانوي (بوّابة FCM §5 قائمة).
2. ⛔ **لا قياسَ عميل.** صفرُ عقد، صفرُ فاتورة، صفرُ عرضٍ مُرسَل، صفرُ سكّةٍ معلَنةِ
   الجاهزية — كلها حقولٌ في رأس ملفّ القياس.
3. ⛔ **لا رقمَ إيرادٍ ولا تسعير.** `revenue_claim = NONE` · `invoices_issued = 0`.
4. ⛔ **لا خطَّ عرضٍ ثامن** في
   [`OFFER_CATALOG.json`](../commercial/OFFER_CATALOG.json) — بقي **سبعة**. هذه
   الدفعة لا تُنتج عرضاً؛ تُنتج **قائمةَ ما يجوز أن يدخل عرضاً**.
5. ⛔ **سطحُ الاقتباس محصورٌ في الادّعاءات المُدرجة في `CLAIMS` (17).** الحكمُ
   «اثنان من سبعة عشر قابلاً للاقتباس» وصفٌ لسطحٍ مُعلَن لا إحصاءٌ للمستودع —
   وK3 هو شرطُ قتله، **وقد أُطلِق فعلاً على هذه الدفعة** (§4.1).

---

## 0 — الجملةُ البحثية، ولماذا هذا المحور الآن

### 0.1 الجملة (قابلةٌ للدحض)

> **السحبُ في هذا المستودع محلّيٌّ لا متتابع: `FCM` (2026-09-15) سحبت نموذجَ
> ميزانية الأيام، فبقيت خمسةُ ادّعاءاتٍ مشتقّةٍ منه حيّةً في ثلاثة ملفاتِ قياسٍ
> وفي ثلاث أدواتٍ قابلةٍ للتشغيل. من سبعة عشر ادّعاءً مُدرَجاً، اثنان فقط قابلان
> للاقتباس كما كُتبا (11.76%). والأثرُ التجاريُّ ليس أكاديمياً: وثيقةُ عرضٍ تقتبس
> رقماً مسحوباً تُسقِط الصفقةَ والمصداقيةَ معاً.**

### 0.2 لماذا الآن — الترتيبُ لا المصادفة

`FCM` دخلت المستودع في **آخر commit** على هذا الفرع (`8489067`). أيّ دفعةٍ تُكتب
بعدها ولا تحسب تتابعَها تكتب فوق أرضٍ مُحرَّكة. والقياسُ هنا يُغلق تلك الفجوة قبل
أن تُبنى عليها دفعةٌ ثانيةَ عشرة.

### 0.3 تعريفان جديدان — و⛔ لم يُدَّع أنّهما كانا موجودَين

- **سطحُ الاقتباس** (quotable surface): مجموعةُ الادّعاءات المُسندة مع أحكامها من
  مجموعةٍ مغلقة — لا «موثوقٌ عموماً».
- **بوّابةُ جاهزية السكّة** (rail-readiness gate · **T4**): الترتيبُ الإلزامي
  `جاهزيةُ السكّة ← إصدارُ الفاتورة ← دفعُ المشتري ← الترحيلُ اللحظي`.
  ⟦تصحيحٌ مؤرَّخ 2026-09-15 — إضافةٌ لا حذف⟧ كُتب هنا في المسودّة الأولى «⛔ لا
  وجودَ لها في أيّ دفعةٍ سابقة» — وهذا **خاطئٌ ودُحض**: الأسبقيةُ في
  `WOD_MEASUREMENTS.json → bank_export_declaration.note_ar` = «K2: لا عقدَ قبل
  إيداع التصريح»، والمتطلّبُ `export_declaration` داخل المجموعة الإلزامية **لـA0
  نفسها**. سببُ الخطأ أنّ البحثَ النصّيَّ سأل عن «rail readiness / قبل الإرسال» ولم
  يسأل عن `export_declaration` / «قبل التوقيع». ⇒ **K3 أُطلِق على هذه الدفعة** (§4.1).

---

## 1 — سجلُّ السحب: ستّةُ أحداث، كلٌّ بنصّه ومصدره

| # | النموذجُ المُبطَل | التاريخ | المصدرُ على القرص | البديل |
|---|---|---|---|---|
| **W1** | `RSM_DAY_BUDGET` — `gap = 120 − Σ days` وجدولُ الـ24 تركيبة | 2026-09-15 | `research/fx-hard-currency/FCM.md §2.1` | S_B |
| **W2** | `RSM_NET60_CEILING` — «السقفُ العملي net-60» | 2026-09-15 | `FCM.md §2.2` | S_A |
| **W3** | `RSM_PENALTY_MULTIPLE` — غرامةُ 1×–2× التخمينية | 2026-09-15 | `FCM.md §2.3` | S_D |
| **W4** | `RSM_48H_DOMICILIATION` — قاعدةُ الـ48 ساعة | 2026-09-15 | `FCM.md §2.4` | S_C |
| **W5** | `RETENTION_50_PERCENT` — «50% من عوائد التصدير» | 2026-09-13 | `ASSURANCE_CONVERGENCE_SPEC_v1.md C1` | S_F |
| **W6** | `NO_MILLENNIUM_VERDICT` — «⛔ لا مسائل الألفية» | 2026-09-12 | `FRONTIER_CLAIM_LEDGER_2026-09.md A3 ⟦تصحيحٌ مؤرَّخ⟧` | — |
| **W7** | `T4_NO_PRIOR_ART` — «بوّابةُ T4 لا أسبقيةَ لها» (**ادّعاؤنا نحن**) | 2026-09-15 | `WOD_MEASUREMENTS.json → bank_export_declaration.note_ar` (K2) | S_B |

**قاعدةُ السجل:** ⛔ لا يُضاف حدثٌ بلا نصٍّ مقتبسٍ حرفاً ومصدرٍ على القرص — وهو
فرضٌ في الكود (`Withdrawal.__post_init__` يرفع `RetractionError`) لا في التوثيق وحده.

---

## 2 — القيودُ البديلة: ما يحلّ محلّ المسحوب، بالمادة نفسها

السحبُ بلا بديلٍ ثقب. البديلُ هنا مُسندٌ بالمادة التي سحبت:

| # | القيدُ النافذ | المرجع | الأثرُ التصميمي |
|---|---|---|---|
| **S_A** | سقفُ أجل الائتمان 120 يوماً · 180 بتأمينٍ مُسبق | النظام 26-02 م2 · الجريدة ن°58 | قيدٌ على **بند العقد** لا على توقيت الترحيل ⇒ الآجالُ المقبولة {t ≤ 120} ∪ {120 < t ≤ 180 ∧ مؤمَّن}. ⛔ **لا يستلزم «أقصر = أفضل»** |
| **S_B** | الترحيلُ مستحقٌّ **يوم يدفع العميل** | النظام 26-02 م2 (يعيد صياغة م61/07-01) | الالتزامُ يلحق بلحظةٍ يتحكّم فيها **المشتري** ⇒ المتغيّرُ التصميمي **جاهزيةُ السكّة** لا أجلُ الدفع |
| **S_C** | إعفاءُ الخدمات الرقمية والناشئة و«غير التجّار» من التوطين، ويُستعاض بتصريح | م57/07-01 معدَّلاً بـ21-01 | بوّابةُ الدخول **تصريحٌ** لا فتحُ حساب ⇒ كلفةُ الاختبار تنزل، وأجلُه لا |
| **S_D** | الجزاءُ **تحصيلٌ بالدينار** لا غرامة | م67/07-01 معدَّلاً بـ21-01 | الخطرُ ليس غرامةً تُقدَّر بل **فقدانُ العملة الصعبة نفسه** ⇒ الخرقُ يُحوِّل الإيرادَ الصعب محلياً (محرَّمٌ هدفاً · §04 D-273) |
| **S_E** | النصُّ الحرفيُّ لم يُستخرج | `FCM.md §5` | ⛔ لا رقمَ تنظيمياً في عرضٍ قبل التقاطٍ من `joradp.dz` |
| **S_F** | الاحتفاظُ 100% لا 50% | التعليمة 06-2021 م4 + بيان 2021-07-18 | «100%» قيدُ **إنفاق** (80% استيراد / 20% ترقية) لا حريةُ تصرّف |

### 2.1 القيدُ محسوباً لا مروياً

```
t = 0    → ADMISSIBLE              (الدفعُ المُقدَّم — أقوى تجارياً وأقلّ تعرّضاً)
t = 15…120 → ADMISSIBLE
t = 150, 180 → NEEDS_CREDIT_INSURANCE   (وADMISSIBLE_INSURED_ONLY إن وُجد التأمين)
t = 270, 360 → PROHIBITED
```

### 2.2 النتيجةُ المحورية: الهامشُ كميةٌ **غيرُ معرَّفة** لا صغيرة

تحت S_B ينشأ الالتزامُ لحظةَ دفع المشتري، فلا يبقى مقدارٌ يُطرح منه أجلُ السداد:

```
repatriation_slack_days() → {"slack_days": None, "reason": "…RSM_DAY_BUDGET مسحوب (W1)…"}
buffer_days(30)           → raises WithdrawnModelError
```

`None` لا صفراً (D-212) — و`buffer_days()` **مُبقاةٌ بالاسم نفسه عمداً** وترفع دائماً:
أيُّ كودٍ قديمٍ أو وثيقةٍ تستدعيها تسقط فوراً بدل أن تُنتج رقماً مسحوباً بهدوء.
هذا هو الفارض، لا التعليق.

---

## 3 — سطحُ الاقتباس: 15 ادّعاءً، حكمٌ لكلٍّ

| الحالة | العدد | المعنى |
|---|---|---|
| `QUOTABLE` | **2** | يُقتبَس كما هو (C15 · C16) |
| `SURVIVES_NARROWER_GROUND` | **4** | ينجو مضيَّقاً — يُعاد إسنادُه ويُسقَط الرقمُ المسحوب |
| `BLOCKED_PENDING_TEXT` | **3** | سالمٌ بنيوياً، محجوبٌ حتى النصّ الرسمي (S_E) |
| `RECLASSIFIED` | **2** | النموذجُ سليمٌ في سؤالٍ آخر — ⛔ لا يُحذَف |
| `VOID_WITHDRAWN` | **6** | ⛔ كميةٌ غيرُ معرَّفة — تُسحَب من أيّ عرض (ومنها C17 ادّعاؤنا نحن) |
| **المجموع** | **17** | **11.76% قابلٌ للاقتباس كما كُتب** |

### 3.1 المسحوبُ الخمسة

| # | الموضعُ على القرص | لماذا يسقط |
|---|---|---|
| C1 | `AHW_MEASUREMENTS.json → contract_terms.rows[*].per_ref[*].buffer_days` | `cap − term` هو `gap` المسحوب حرفاً |
| C3 | `shared/research/assurance_window.py:634 buffer_days()` | الدالةُ التي تُنتج C1 |
| C4 | `shared/research/fx_rail.py:375→392 margin_days_to_120` | الطرحُ نفسه |
| C6 | `fx_rail.py:418 deadline = service_realized_day + 120` (خرق `PAID_AFTER_120`) | موعدٌ يبدأ من إنجاز الخدمة = ميزانيةُ أيام |
| C14 | `AHW_MEASUREMENTS.json → contract_terms.min_buffer_days` | عتبةٌ على كميةٍ غيرِ معرَّفة |

### 3.2 الناجي المضيَّق — أهمُّ تصحيحٍ في الدفعة

| # | الادّعاء | السندُ الساقط | السندُ الباقي |
|---|---|---|---|
| C2 | `robust_under_all_refs` (AHW) | W1 | **S_A** |
| C10 | «عقودُ خدمات ≤ 45 يوماً صامدة» (VERA N5) | W1 | **S_A** |
| C11 | «نموذج RSM: 11 من 24 تركيبة في خرق» (WOD A9) | W1 | **K5** (كيانٌ أجنبي · الأمر 03-11 م126) |
| C12 | `A9 status = BLOCKED_DOCTRINE` | W1 | **K5** |

> **الخلاصةُ التي تُقتبَس:** «≤ 45 يوماً» **صحيحة** — لكن لأنّ 45 ≤ 120 (سقفُ
> الائتمان)، **لا** لأنّ الهامش 75 أو 90 يوماً. والأرقامُ 90/75/276 في
> `AHW_MEASUREMENTS.json` **غيرُ قابلةٍ للاقتباس**. كذلك حكمُ المنع على A9 يبقى
> قائماً **على K5 وحده** — ويجب إعادةُ إسناده كتابةً، لأنّ عرضاً يستند إلى سندٍ
> مسحوبٍ قابلٌ للنقض وإن كانت خلاصتُه صحيحة.

### 3.3 المعادُ تصنيفُه — حذفُه إتلافُ معرفة

| # | الأداة | كانت تُسمّى | تصير |
|---|---|---|---|
| C8 | `portable_trust.repatriation_breach_risk()` | مقياسُ خطرِ **الامتثال** | مقياسُ خطرِ **تدفّقٍ نقدي** (متى يدفع المشتري) |
| C9 | `portable_trust.contract_term_ceiling()` | «الرقمُ الذي يُكتب في العقد» | سقفُ **تفاوضٍ نقدي** — لا بندُ امتثال |

الحسابُ اللوغاريتميُّ الطبيعي سليم؛ الخطأُ في **اسم السؤال**. ولهذا رُتِّبت
`RECLASSIFIED` قبل `VOID_WITHDRAWN` في `adjudicate()` — ولو سبقتها لأسقطت مقياسَين
صالحَين، وهذا مُثبَتٌ ببرهانٍ سلبيّ في الاختبارات.

---

## 4 — البوّابةُ T4: ترتيبٌ قائمٌ في WOD، مُشدَّدٌ هنا

```
جاهزيةُ السكّة + تصريحُ م57   ←   إصدارُ الفاتورة   ←   دفعُ المشتري   ←   الترحيلُ (لحظي)
```

| الحالة | الحكم |
|---|---|
| فاتورةٌ صدرت والسكّةُ غيرُ جاهزة | ❌ `INVOICE_BEFORE_RAIL` |
| فاتورةٌ صدرت والتصريحُ غائب | ❌ `DECLARATION_MISSING` |
| سكّةٌ جاهزة + تصريحٌ مقدَّم + فاتورة | ✅ تمرّ |
| لا فاتورةَ بعد | ✅ تمرّ (القيدُ على الإصدار لا على الوجود) |

**لماذا شُدِّدت؟** لأنّ S_B تجعل الالتزامَ لحظياً عند الدفع. فالفاتورةُ تُصدر
**قبل** أن يتمكّن المشتري من الدفع، وإذا لم تكن السكّةُ جاهزةً لحظةَ وصول الحصيلة
وقع الخرقُ **بلا خطأٍ إجرائيٍّ من أحد** — وتحت S_D يكون الجزاءُ تحويلاً إلى
الدينار، أي فقدانَ العملة الصعبة نفسها.

### 4.1 ⟦تصحيحٌ مؤرَّخ 2026-09-15 — إضافةٌ لا حذف⟧ الأسبقيةُ موجودة، وK3 أُطلِق

| | ما قالته WOD (2026-09-14) | ما تضيفه هذه الدفعة |
|---|---|---|
| **الأصل** | `bank_export_declaration` (effort 0) — «K2: لا عقدَ قبل إيداع التصريح — شرطٌ لا بندٌ اختياري» | ⛔ لا شيء — الأسبقيةُ كاملة |
| **المُحفِّز** | قبل **التوقيع** | قبل **إصدار الفاتورة** (تشديدٌ بمقتضى S_B) |
| **المُحتوَى** | تصريحٌ شرطٌ للإعفاء من **التوطين** | تصريحُ **م57** (وصفُ المشروع + سعرُ الوحدة + تاريخُ النشر) — لأنّ S_C أثبتت الإعفاءَ أصلاً |
| **الموضع** | متطلّبٌ في مجموعة A0 | **على المسار الحرج** إلى T70 (T78 ← T70) |

> **الدحضُ على أنفسنا:** سُجِّل `W7 / T4_NO_PRIOR_ART` في سجلّ السحب، والادّعاءُ
> `C17` موسومٌ `VOID_WITHDRAWN`، و`K3.state = FIRED_ON_SELF`. والسببُ الجذريُّ
> منهجيٌّ لا إملائي: **البحثُ النصّيُّ عن مرادفٍ ليس دليلاً على الغياب** — والأداةُ
> التي تفحص «هل يعرف القرصُ هذا؟» يجب أن تسأل عن **المعرّفات** لا عن العبارات.

⛔ ليست رأياً قانونياً؛ تشديدُ ترتيبِ تبعيّةٍ قائمٍ (K2) بمقتضى S_B وS_C.

---

## 5 — المسارُ الحرج: مَن يُغلق البطاقة، لا كم تكلّف

### 5.1 القياس

14 بطاقةً مفتوحة على القرص (T65–T76 من FIV/WOD + T77/T78 من هذه الدفعة):

| المُنفِّذ | العدد | | المسار | العدد |
|---|---|---|---|---|
| `RESEARCHER` | **8** | | `CH` (قنوات FIV) | 6 |
| `OWNER` | **3** | | `A0` (الفاتورة الأولى) | 5 |
| `WORLD` | **3** | | `FX` (السكّة التنظيمية) | 3 |

### 5.2 النتيجةُ المقرِّرة

```
sendable_path("T70") → minimum_set = [T66, T68, T77, T78, T70]
                       length = 5 · owner_free = True
                       by_executor = {OWNER: [], RESEARCHER: [T66, T68, T77, T78], WORLD: [T70]}
```

> **أقصرُ مسارٍ إلى أوّل فاتورةٍ مدفوعة لا يمرّ بالمالك إطلاقاً.** أربعُ بطاقاتٍ
> بيد الباحث (صفرُ إنفاق)، والخامسةُ بيد العالم (مُشترٍ واحد). وبالمقابل
> `sendable_path("T73")` ⇒ `owner_free = False` ومحجوبٌ عند **T71**.

هذا فصلٌ حاسم: **مسارُ القنوات (CH) محجوبٌ بالمالك، ومسارُ الفاتورة الأولى (A0)
ليس كذلك** — وانتظارُ ردّ T71 لا يُبرّر تعطيلَ A0.

### 5.3 التشابكُ غيرُ البديهي

| الزوج | منفصلان بالبطاقات | منفصلان بالإغلاق |
|---|---|---|
| A0 ↔ CH | ✅ | ✅ (انفصالٌ تام) |
| A0 ↔ FX | ✅ | ❌ — **يتشابكان عبر T77** |

> أي أنّ استخراجَ نصّ الجريدة الرسمية (T77) — وهو عملٌ بحثيٌّ بحت — **صار على
> المسار الحرج لأول دولار**، لأنّ بوّابةَ T4 تشترط إعلانَ جاهزية السكّة (T78)
> الذي يستند إلى النصّ.

---

## 6 — شروطُ القتل (K1–K5) — مجموعةٌ مغلقةٌ لهذه الدفعة

⚠️ **الترقيمُ لكلّ دفعة** لا تسلسلٌ عامّ (عرف WOD/AHW/AR_FR — ولكلٍّ K1–K5 خاصّةٌ
بها). ولكلٍّ برهانٌ سلبيٌّ في `tests/shared/test_rcl_retraction_cascade.py`.

| # | الشرط | الأثر | المُلاحَظ |
|---|---|---|---|
| **K1** | يُستخرج نصُّ م61 فيُثبت **ميزانيةَ أيام** | W1 باطلة، ويعود `buffer_days` معرَّفاً، وتُلغى T4 | اقتباسٌ حرفيٌّ من `joradp.dz` (T77) |
| **K2** | يدفع مشترٍ وتصل الحصيلةُ بلا تصريحٍ ولا خرق | S_C/S_D أضعفُ من المعلن، وتُنزَل T4 إلى ملاحظة | إيصالُ تحويلٍ + كشفُ حساب (T70) |
| **K3** | يظهر ادّعاءٌ على القرص تعتمد عليه الدفعةُ ولم تلتقطه الأداة | السطحُ ناقص، ولا يُقتبَس «x من y» | **`FIRED_ON_SELF`** — دُحض ادّعاءُ الجِدّة بـK2 (§4.1) |
| **K4** | تُثبت أنّ A0 ليست فئةَ الفاتورة الأولى (C15) | المسارُ الحرج يُعاد حسابُه على فئةٍ أخرى | قياسُ WOD مُعاداً |
| **K5** | تُغلق البطاقاتُ كلُّها ولا يُرسل عرضٌ واحد | العائقُ ليس المعرفة بل **الفاعل** (F-06) | سجلُّ إرسالٍ فارغ بعد T66 وT68 وT78 |

---

## 7 — البطاقاتُ التالية (كلُّها بلا إنفاق)

| # | البطاقة | المُنفِّذ | معيارُ الخروج |
|---|---|---|---|
| **T77** | التقاطُ نصَّ م61 + م57 + م67 من `joradp.dz` — يُغلق S_E ويحرّر C5/C7/C13 | باحث | اقتباسٌ حرفيٌّ بصفحةٍ وتاريخ، أو إسقاطٌ مُعلَّل |
| **T78** | إعلانُ جاهزية السكّة + تقديمُ تصريح م57 — بوابة T4 | باحث | تصريحٌ مُقدَّمٌ بمُعرَّف، أو إعلانُ تعذُّرٍ مؤرَّخ |
| **T79** | إعادةُ إسناد C2/C10/C11/C12 كتابةً في مواضعها (إضافةٌ لا حذف) | باحث | سطرٌ مُؤرَّخ في كلٍّ من AHW/VERA/WOD |
| **T80** | وسمُ C1/C3/C4/C6/C14 `VOID_WITHDRAWN` في مواضعها — ⛔ بلا حذفٍ للنصّ | باحث | وسمٌ مرئيٌّ بجانب الرقم |
| **T81** | إعادةُ تسمية مخرَجَي `portable_trust` (C8/C9) إلى سؤالهما الحقيقي | باحث | اسمٌ جديد + سطرُ سبب، والاختباراتُ خضراء |

T79/T80/T81 تنفيذٌ لنتيجة هذه الدفعة، لا معرفةٌ جديدة — وتفصلها عنها صراحةً.

---

## 8 — كيف تتحول إلى عملة صعبة (وما لا تدّعيه)

```
سطحُ اقتباسٍ نظيف → عرضٌ لا يُنقَض → إرسال → ردّ/صمت → فاتورة
```

القيمةُ العملية ليست رقماً أعلى بل **منعُ خسارة**: عرضٌ يقتبس `buffer_days = 90`
اليوم قابلٌ للنقض بسطرٍ واحدٍ من FCM. وأمّا المسارُ فمحسوب: **خمسُ بطاقات، أربعٌ
بيد الباحث، وصفرُ إنفاق، ولا مالك**.

**⛔ صفرُ عقد · صفرُ فاتورة · صفرُ عرضٍ مُرسَل · صفرُ سكّةٍ جاهزة · `GATE_C = ABSENT`.**

---

*باحث مستقل — 2026-09-15 — دفعة حادية عشرة: RCL — إضافة لا حذف. ودحضٌ على أنفسنا:
ستّةُ ادّعاءاتٍ في ثلاثةِ ملفاتِ قياسٍ وثلاثِ أدواتٍ كانت ستبقى حيّةً بعد سحبِ
نموذجها لو لم يُحسَب تتابعُها.*
