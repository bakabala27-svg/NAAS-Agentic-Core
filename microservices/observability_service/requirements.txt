# Main Requirements - Use specific requirement files for different environments:
# - requirements-prod.txt: Production dependencies only
# - requirements-dev.txt: Development tools (includes prod)
# - requirements-test.txt: Testing tools (includes prod)

# Core Production Dependencies
fastapi==0.109.2
uvicorn==0.27.1
# starlette # Let fastapi manage starlette version
SQLAlchemy==2.0.25
sqlmodel==0.0.14
alembic==1.13.1
asyncpg==0.29.0
aiosqlite==0.17.0
psycopg2-binary==2.9.9
pydantic>=2.11.5,<3.0.0
pydantic-settings==2.3.0
PyJWT==2.13.0
passlib==1.7.4
bcrypt==3.2.0
argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0
cryptography>=50.0.0
Authlib==1.6.12
httpx==0.27.0
python-dotenv==1.2.2
python-multipart==0.0.31
Jinja2==3.1.6
typer==0.20.0
tenacity==8.2.3
langgraph==1.0.10rc1
langchain-core>=0.2.39

# Supporting Libraries
click>=8.0.0
greenlet>=3.0.0
h11>=0.14.0
httpcore>=1.0.0
httptools>=0.6.0
anyio>=4.0.0
sniffio>=1.3.0
certifi>=2023.0.0
idna>=3.0
inflection==0.5.1
MarkupSafe>=2.1.0
shellingham==1.5.4
uvloop>=0.19.0
watchfiles>=0.21.0
websockets>=12.0
beautifulsoup4==4.12.3
soupsieve==2.9
charset-normalizer>=3.0.0
annotated-types>=0.6.0
python-json-logger==4.0.0
setproctitle==1.3.7
six==1.17.0
wrapt==1.17.3
Deprecated==1.2.18
filelock>=3.13.0
platformdirs>=4.0.0
typing_extensions>=4.9.0
# Versions are centralized in constraints.txt for deterministic installs.
pypdf
python-docx==1.2.0
openpyxl==3.1.5
pandas==2.3.3
pillow==12.3.0
pytesseract==0.3.13
pyyaml==6.0.1

# AI & Search Stack
# Pin huggingface-hub to a stable version compatible with the ecosystem
# Note: Do not use [inference] extra as it is missing in 1.x versions and causes backtracking
huggingface-hub>=0.23.0
# sentence-transformers removed 2026-08-01 (D-205): zero `sentence_transformers`
# imports anywhere under microservices/observability_service/. It pulled torch (~2GB) into an
# image that never used it. research_agent is the only real consumer.
dspy-ai==2.5.43
openai==1.55.0
# llama-index meta-package removed to reduce bloat/conflicts
llama-index-core==0.14.13
llama-index-readers-file
llama-index-vector-stores-supabase
llama-index-embeddings-huggingface
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
PyYAML
passlib
argon2-cffi
vecs==0.4.5
flupy==1.2.3
# ⛔ REMOVED AS *UNSOLVABLE*, NOT RETARGETED — the D-205 remedy (ISS-202).
# `litellm==1.50.0` is no longer published: the PyPI index jumps 1.27.1.x → 1.53.1,
# so every image build of this service died at `pip install` (reproduced with the
# image's own resolver, pip 25.0.1). Nothing here imports litellm — the pin was a
# second brake on `openai==1.55.0`, not a dependency. Raising the pair to litellm
# >=1.8x/openai 2.x is still an ADR across six services, and is NOT done here.
# litellm still installs transitively (via dspy) wherever the runtime wants it.
dspy==3.0.4b1
llama-index-workflows==2.14.0
