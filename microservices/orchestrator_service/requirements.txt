fastapi==0.109.2
uvicorn==0.27.1
pydantic>=2.8.0,<3.0.0
pydantic-settings==2.2.1
sqlmodel==0.0.16
asyncpg>=0.29.0
redis>=5.0.1
tenacity>=8.2.3
python-multipart>=0.0.32
httpx>=0.27.0,<0.28.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
dspy==3.0.4b1
openai>=1.12.0
# R0.2 (D-172): the Postgres checkpointer stack MUST be installed and pinned to
# a mutually-compatible set, or `from langgraph.checkpoint.postgres.aio import
# AsyncPostgresSaver` fails → the guard silently degrades to MemorySaver (a
# hidden fallback). langgraph range mirrors root requirements.txt (CI-proven).
langgraph>=1.2.11,<2.0.0
langgraph-checkpoint>=4.2.0,<5.0.0
langgraph-checkpoint-postgres>=3.1.2,<4.0.0
psycopg[binary,pool]>=3.1,<4.0
llama-index-core==0.14.13
python-json-logger>=4.0.0
opentelemetry-api
opentelemetry-sdk
prometheus-client>=0.20.0
# D-205: migrations/env.py imports alembic at module level, and alembic was in
# no service requirements file — so `alembic upgrade` could never run inside this
# image. Not a startup blocker (the app does not import it), which is exactly why
# it went unnoticed while no image was ever built.
alembic>=1.13.1
