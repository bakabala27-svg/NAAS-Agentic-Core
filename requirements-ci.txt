# Lightweight CI dependency set for GitHub Actions.
# Intentionally avoids the full production AI/search stack when a narrower set
# is sufficient for repository validation, guardrails, and the main test job.

# Core application/runtime dependencies used by tests and guardrails
# ⛔ NOT 0.141.1 (#2290 proposed it): it resolves Starlette to 1.6.0, whose
# TestClient no longer swallows the cancellation of a server task that is still
# running when the client leaves a `websocket_connect` block:
#     starlette/testclient.py:131 in __exit__ -> concurrent.futures._base.CancelledError
# The cancelled task is real and ours: app/api/routers/customer_chat_support/
# turn_lifecycle.py:230 spawns the BKT evaluation with `asyncio.create_task` and
# never awaits or cancels it, so it is still pending when the turn ends. Starlette
# 1.6 reports that leak instead of hiding it. Fixing it means changing how a
# student's turn owns its background work — governed by D-074 (BKT must never
# break a turn) and §6.5 (one terminal frame per turn) — so it is its own change.
# Measured: with 0.141.1 the WS error-contract test fails on teardown; with
# 0.109.2 the same file is 5 passed. uvicorn, pydantic, websockets, httptools,
# watchfiles and python-multipart all keep #2290's bumps.
# Tracked for removal in #2303 (BKT task ownership).
fastapi==0.109.2
uvicorn==0.52.3
SQLAlchemy==2.0.52
sqlmodel==0.0.39
alembic==1.19.1
asyncpg==0.31.0
# ⛔ NOT 0.22.1 (#2293 proposed it): the test harness keeps one cached engine
# (tests/conftest_support/registry.py) across a per-test `event_loop`, and under
# 0.22.1 a connection whose loop has gone away no longer reconnects — it raises
#     ValueError: no active connection      (aiosqlite/core.py:137)
#   -> sqlite3.OperationalError: no active connection
# at fixture setup for whichever test follows a WebSocket test. Measured on this
# branch: tests/api/test_chat_event_protocol_error_contract_integration.py is
# 1 failed + 1 error on 0.22.1 and 5 passed on 0.17.0, nothing else changed.
# Moving to 0.22.1 means giving the harness one engine per loop, which is a
# change to shared test infrastructure (D-258), not a dependency bump.
# Tracked for removal in #2303 (engine cached per loop).
aiosqlite==0.17.0
psycopg2-binary==2.9.12
pydantic>=2.13.4,<3.0.0
pydantic-settings>=2.15.0,<3.0.0
PyJWT==2.13.0
passlib==1.7.4
# ⛔ NOT 5.0.0 (#2297 proposed it): passlib 1.7.4 pins its bcrypt backend to a
# `detect_wrap_bug` probe that hashes a >72-byte password. bcrypt 4.x truncated
# and warned; bcrypt 5.0.0 raises `ValueError: password cannot be longer than 72
# bytes` — so the backend never finishes loading and EVERY hash and verify call
# raises. Measured: passlib 1.7.4 + bcrypt 5.0.0 -> ValueError at CryptContext
# .hash(); + bcrypt 4.3.0 -> hash and verify both succeed. app/security/passwords
# .py and microservices/user_service list "bcrypt" in their CryptContext schemes,
# so this is every login, not an edge case. 4.3.0 is the newest 4.x. Moving to
# bcrypt 5 means leaving passlib (unmaintained since 2020), not bumping a pin.
bcrypt==4.3.0
argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0
cryptography>=50.0.0,<51.0.0
Authlib==1.8.0
httpx==0.27.0
python-dotenv==1.2.2
python-multipart==0.0.32
Jinja2==3.1.6
typer==0.20.0
tenacity==8.2.3
redis>=5.0.0
websockets>=17.0.1
beautifulsoup4==4.12.3
numpy>=1.26.0
pyyaml==6.0.1

# AI/runtime imports exercised by the current test suite
# ⛔ The LangGraph/LangChain floors below are held deliberately (see the coupled
# checkpoint pair near the end of this file). #2294 proposed langgraph>=1.2.11 +
# langchain-core>=1.5.5 + checkpoint 4.x, which is an engine upgrade needing an
# ADR — and it is also unsatisfiable next to `websockets>=17.0.1` (#2290):
# langgraph 1.2.11 requires langgraph-sdk>=0.4.2, which caps websockets at <17.
# Measured with `uv pip compile --python-version 3.12`, which reports
# "your requirements are unsatisfiable" — i.e. every CI job dies at pip install.
langgraph>=1.2.11,<2.0.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
llama-index
llama-index-core>=0.14.23
llama-index-workflows
llama-index-embeddings-huggingface
llama-index-vector-stores-supabase
openai
sentence-transformers
dspy
json-repair
opentelemetry-api
opentelemetry-sdk
prometheus-client>=0.26.0

# Missing dependencies for CI test environments
python-json-logger
redis-om
asgi-lifespan
argon2_cffi
docker

# File/content tooling imported in tested code paths
pypdf
python-docx==1.2.0
openpyxl==3.1.5
pandas==2.3.3
pillow==12.3.0
pytesseract==0.3.13

# Test tooling
# ⛔ The pytest quartet is held (#2296 proposed pytest 9.1.1 / pytest-asyncio
# 1.4.0 / pytest-cov 7.1.0 / pytest-env 1.7.0). pytest-asyncio 1.0 stopped
# honouring a user-defined `event_loop` fixture, and this suite's loop model
# depends on it: `tests/conftest.py:90` owns `event_loop`, and the autouse
# `db_lifecycle` fixture drives `_reset_db()` on it via `run_until_complete`.
# Under 1.4.0 the fixtures keep using that loop while the test body runs on a
# loop pytest-asyncio creates, so the cached aiosqlite engine is used from a
# loop it was not created on:
#     ValueError: no active connection  (aiosqlite/core.py:137)
#   -> sqlite3.OperationalError: no active connection
#   raised in tests/conftest_support/lifecycle.py:40, i.e. at fixture setup for
#   every DB-touching test, and the run deadlocks on the first TestClient one.
# Realigning loop ownership means rewriting the shared harness that ~4,800
# tests depend on (D-258 governs that file); it is its own change with its own
# issue, not a dependency bump. python-dotenv follows: its only reason to move
# was pytest-env 1.7.0's floor.
# Tracked for removal in #2303 (loop ownership + unregistered hooks).
pytest==9.0.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-env==1.1.3
pytest-factoryboy==2.8.1
pytest-timeout==2.4.0
coverage==7.15.4
factory-boy==3.3.3
Faker==40.36.0
hypothesis==6.165.10
sortedcontainers==2.4.0
attrs==25.4.0
# R0.2 (D-172): pinned range (was unpinned) — reproducible checkpointer stack.
# ⛔ THESE TWO MOVE TOGETHER OR NOT AT ALL — and both are capped by `langgraph<2.0.0`
# at line 36. Dependabot has now broken this pair twice in two days:
#   4b0db53 raised -postgres to >=3.1.2 alone  → ResolutionImpossible
#   91509a0 raised -checkpoint to >=4.2.0      → ResolutionImpossible again
# `langgraph-checkpoint-postgres` 3.x needs `langgraph-checkpoint>=3`; `-checkpoint`
# 4.x needs `langgraph>=1.2`, which line 36 forbids. Either raise alone and pip fails
# outright — no resolution, so every job dies at `pip install` before one test runs.
# That is exactly how main went red (guardrails + test-monolith + test-microservices).
# 2.x is the combination proven green on b1db497, and it is what holds this project on
# LangGraph 1.1.10. Raising them means moving to langgraph 2.x: an engine upgrade with
# an ADR, not a dependency bump. `microservices/orchestrator_service/requirements.txt`
# carries the identical coupled pair — change both or neither.
langgraph-checkpoint>=4.2.0,<5.0.0
langgraph-checkpoint-postgres>=3.1.2,<4.0.0
psycopg-binary
